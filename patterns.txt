@title:
@author:
@email:
@creationDate:
@github: 

@imports -> specify name of imports 
(good to have link to download or documentation of imports)


@var -> explication de quoi ca sert
@var -> *  (yaane name of variable self explanatory)

@func -> explication de quoi ca sert
si on ecrit rien, ca prend la func either way (meaning its self explanatory)

@sectionStart -> id of section
@sectionEnd -> id of section (same as start pr que ca link)

## (comment on line of code)